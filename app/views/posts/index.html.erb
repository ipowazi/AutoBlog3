<ol>
<% @posts.each do |post| %>
	<li><%= post.title %></li>	
	<p class="subtext">Posted by <%= post.user[:email] %> <%= time_ago_in_words(post.updated_at) %> ago | <%= post.comments.count %> <%= "comment".pluralize(post.comments.count) %> </p>
<% end %>
</ol>