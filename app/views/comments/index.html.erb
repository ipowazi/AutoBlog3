<% @comments.each do |comment| %>
	<p class="toptext"><%= comment.user.name %> <%= time_ago_in_words(comment.updated_at) %>  ago | on <%= link_to comment.post.title, comment.post.url %></p>
	<p class="subbody"><%= comment.body %></p>
<% end %>
